#include <string>
#include <vector>
#include <algorithm>

using namespace std;

bool comp(const int num1, const int num2)
{
    string s1 = to_string(num1) + to_string(num2);
    string s2 = to_string(num2) + to_string(num1);
    if(s1 < s2) return num2 > num1
}

string solution(vector<int> numbers) {
    string answer = "";
    
    return answer;
}